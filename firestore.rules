rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /anime/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin') 
        || request.auth.token.email in ['simoonabdulla@gmail.com', 'kamaluddin124578@gmail.com']
      );
    }
    match /episodes/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin') 
        || request.auth.token.email in ['simoonabdulla@gmail.com', 'kamaluddin124578@gmail.com']
      );
    }
    match /comments/{document=**} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && (
        request.auth.uid == resource.data.userId || 
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin') || 
        request.auth.token.email in ['simoonabdulla@gmail.com', 'kamaluddin124578@gmail.com']
      );
    }
    match /watchlist/{document=**} {
      allow read, delete: if request.auth != null && resource != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && request.resource != null && request.auth.uid == request.resource.data.userId;
    }
    match /watchProgress/{progressId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && (
        (request.resource != null && request.resource.data.userId == request.auth.uid) || 
        progressId.split('_')[0] == request.auth.uid
      );
    }
    // Default fallback
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
